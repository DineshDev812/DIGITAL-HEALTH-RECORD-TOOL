package com.digitalhealth.repository; import com.digitalhealth.model.Appointment; import org.springframework.data.jpa.repository.JpaRepository; import java.util.*; public interface AppointmentRepository extends JpaRepository<Appointment,Long>{ List<Appointment> findByWorkerIdOrderByAppointmentAtDesc(Long id); boolean existsByWorkerIdAndProviderAndAppointmentAt(Long id,String provider,java.time.LocalDateTime at); }
